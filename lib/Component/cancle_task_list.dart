import 'package:flutter/material.dart';

class CancleTaskScreen extends StatelessWidget {
  const CancleTaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
